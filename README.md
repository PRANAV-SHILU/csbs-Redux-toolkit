# React redux toolkit

==>Provider provides data to react dom from redux store which wraps up main.jsx

=> slice is combination of action & reducer

=> Action takes data from application to store in redux store

=>Reducer keeps data updated like add,delete

=> Redux toolkit is a wrapper of redux which makes code simple,short and easy.